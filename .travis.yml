language: erlang

sudo: false

otp_release:
  - 17.5
  - 18.0
  - 18.2

notifications:
  email: false

addons:
 apt:
   sources:
     - ubuntu-toolchain-r-test
   packages:
     - gcc-4.7
     - g++-4.7

env:
  - CC=gcc-4.7 CXX=g++-4.7

script:
  - "make tests"
