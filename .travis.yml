language: erlang

sudo: false

otp_release:
  - 17.5
  - 18.0
  - 18.2

notifications:
  email: false

addons:
 apt:
   sources:
     - ubuntu-toolchain-r-test
   packages:
     - gcc-4.8
     - g++-4.8

env:
  - CC=gcc-4.8 CXX=g++-4.8

script:
  - "make tests"
